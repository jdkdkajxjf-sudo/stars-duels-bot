{
  "name": "stars-duels-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts",
    "postinstall": "prisma generate",
    "setup:sqlite": "sed -i 's/provider = \"postgresql\"/provider = \"sqlite\"/' prisma/schema.prisma && prisma generate && prisma db push --accept-data-loss",
    "setup:postgres": "sed -i 's/provider = \"sqlite\"/provider = \"postgresql\"/' prisma/schema.prisma && prisma generate && prisma db push --accept-data-loss"
  },
  "dependencies": {
    "@prisma/client": "^6.11.1",
    "dotenv": "^17.4.2",
    "prisma": "^6.11.1"
  },
  "devDependencies": {
    "bun-types": "^1.3.4"
  }
}
